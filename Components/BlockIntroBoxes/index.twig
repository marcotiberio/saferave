<div class="flyntComponent" style="background-color: {{ backgroundColor|e }}" is="flynt-block-intro-boxes">
  <div class="container fullWidthContainer">
    <ul class="items">
      {% for guideline in guidelines %}
      <li class="item">
        <a href="{{ guideline.pageLink }}" class="item-link button button--link">
          {% if guideline.icon or guideline.title or guideline.text %}
          {# <div class="item-icon">
            <img class="content-image lazyload"
              src="{{ guideline.icon.src|resizeDynamic(424, 283) }}"
              srcset="{{ placeholderImage(3, 2) }}"
              data-srcset="
                {{ guideline.icon.src|resizeDynamic(312, 208) }} 312w"
              data-sizes="auto"
              alt="{{ guideline.icon.alt|e }}">
          </div> #}
          <div class="item-title">{{ guideline.title|e }}</div>
          <div class="item-description">{{ guideline.text|e('wp_kses_post') }}</div>
        </a>
      </li>
      {% endif %}
      {% endfor %}
    </ul>
  </div>
</div>
